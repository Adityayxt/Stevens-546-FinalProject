<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Skills | SkillSwap</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
<div class="page-wrapper">
  <header class="nav">
    <a href="/skills" class="logo">SkillSwap</a>
    <nav class="nav-links">
      <a href="/skills">Skills Marketplace</a>
        <a href="/skills/new">Publish Skill</a>
        <a href="/profile/myskills" class="active-link">My Skills</a>
        <a href="/profile/me">My Profile</a>
        <a href="/profile/favorites">My Favorites</a>
        <a href="/auth/logout">Logout</a>
    </nav>
  </header>

  <h1 class="page-title">My Skills</h1>

  <div style="text-align: center; margin-bottom: var(--spacing-xl);">
    <a href="/skills/new" class="btn-primary" style="display: inline-flex; align-items: center; gap: var(--spacing-sm);">
      <span>➕</span>
      <span>Publish New Skill</span>
    </a>
  </div>

  <div class="card-container">
    <% if (skills.length === 0) { %>
      <div class="empty-state">
        <span class="empty-state-icon">📝</span>
        <div class="empty-state-title">No Skills Published Yet</div>
        <p class="empty-state-description">You haven't published any skills yet. Come and publish your first skill!</p>
        <div class="empty-state-actions">
          <a href="/skills/new" class="btn-primary">Publish Skill</a>
        </div>
      </div>
    <% } else { %>
      <% skills.forEach(skill => { %>
        <div class="card">
          <h3 class="card-title"><%= skill.title %></h3>
          <p class="card-desc"><%= skill.description %></p>
          <div class="card-meta">
            <span>Category: <%= skill.category %></span>
          </div>
          <div class="card-meta">
            <span>Posted on: <%= new Date(skill.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
          </div>

          <div class="flex-actions">
            <a href="/profile/edit/<%= skill._id %>" class="btn-secondary">
              <span>Edit</span>
            </a>
            <form action="/profile/<%= skill._id %>?_method=DELETE" method="POST">
              <button type="submit" class="btn-danger">
                <span>Delete</span>
              </button>
            </form>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>

  <footer class="footer">
    © 2025 SkillSwap Skill Exchange Platform | Enabling everyone to be both teacher and student
  </footer>
</div>
</body>
</html>